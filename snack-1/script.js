const names = ['Edoardo', 'Simone', 'Francesco'];

// Stampa in console tutti i nomi
// Risultato: 'Edoardo', 'Simone', 'Francesco'

// Utilizzo un ciclo for per stampare tutti i nomi
  for (let i = 0; i < names.length; i++) {
    let name = names[i];
    console.log(name);
  }

  